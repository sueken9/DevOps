# アンチパターン

## 目的が違う処理を1行にまとめる

目的が異なる処理は、必ず別の行に分けて書く。メソッドチェーンで繋いで1行にしない。

### 例：生成と実行を繋げる

```csharp
// ✗ 悪い：生成と実行が1行に混在している
SmartGarageFactory.Create().Run();

// ○ 良い：目的ごとに行を分ける
var controller = SmartGarageFactory.Create();  // 生成
controller.Run();                              // 実行
```

### なぜ分けるか

| 処理 | 目的 |
|------|------|
| `Create()` | オブジェクトの生成・組み立て |
| `Run()` | 処理の実行・開始 |

目的が違う処理を1行に詰め込むと：
- 何をしている行かが読み取れない
- デバッグ時にどちらで問題が起きたか特定しにくい
- 変数に名前がつかないため、何が生成されたか不明になる

### 判断基準

「この行は何をしているか」を1語で答えられない場合は、行を分ける。

## コメントにHOWを書く

コメントはWHAT（外から見た目的）で書く。HOW（内部の実装・設計手法）を書かない。

| HOW ✗ | WHAT ○ |
|--------|--------|
| STSフローを開始する | ガレージ制御を開始する |
| Factoryで依存関係を組み立てる | スマートガレージ制御システムを構築する |

内部設計（STS・DI・Factory）はコードを読めばわかる。コメントはシステムの目的を伝える。
